<?xml version="1.0" encoding="UTF-8" ?>
<plugin parser="Regexp" file_encoding="UTF_8">
  <mapping data_type="pca_app_launch">
    <name>Pca App Launch</name>
    <description
    ><![CDATA[
Each row is an application launch recorded by the Windows Program Compatibility Assistant, with executable path and high-resolution launch time. Use it as execution evidence and correlate the path with Prefetch, Amcache, UserAssist, or filesystem records. Coverage and retention vary by Windows version, so absence does not exclude execution.
]]>
    </description>
    <short_description>Shows Program Compatibility Assistant application launches with executable paths and precise timestamps.</short_description>
    <category>Logs</category>
    <default_date_pattern value='%Y-%m-%d %H:%M:%S.%f' />
    <!-- regexp specific parameters -->
    <skip_lines value="0" />
    <regexp_fail_policy value="Skip" />
    <regex>
      <![CDATA[
        (?P<executable_path>[A-Z]:[\S\s]+|\\[\S\s]+)\|(?P<timestamp>[0-9]{4}-[0-9]{2}-[0-9]{2} [0-9]{2}:[0-9]{2}:[0-9]{2}.[0-9]{3})
      ]]>
    </regex>

    <!-- Timeline definition -->
    <timeline>
      <timeline_type value="Standard" />
      <description>
        <output_name value="executable_path" />
      </description>
    </timeline>
    <!-- field mapping that match the named regexp -->
    <fields>
      <field
        input="timestamp"
        parser="DateTime"
        description="timestamp of the application launch"
      />
      <field
        input="executable_path"
        parser="String"
        qualifier="FILE_PATH"
        description="full path of the executable launched via PCA"
      />
    </fields>
  </mapping>

</plugin>
