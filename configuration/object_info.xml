<?xml version="1.0" encoding="UTF-8" ?>
<plugin parser="Csv" file_encoding="UTF_8">
  <mapping data_type="objinfo">
    <name>Object Info</name>
    <description
    ><![CDATA[
Each row represents a Windows object-manager namespace entry captured by GetObjInfo, with object type, name, namespace path, symbolic-link target, and link creation time where available. Use it to find suspicious mutexes, devices, or redirections and correlate named objects with processes. The namespace is transient, so the output reflects collection-time state rather than historical activity.
]]>
</description>
    <short_description>Shows Windows object-manager entries with types, namespace paths, symbolic-link targets, and creation times.</short_description>
    <category>Windows Artefacts</category>
    <default_parser value="Ignore" />
    <default_date_pattern value='%Y-%m-%d %H:%M:%S.%3f' />
    <csv_delimiter value="," />

    <!-- Timeline definition -->
    <timeline>
      <timeline_type value="Standard" />
      <description>
        <output_name value="object_name" />
      </description>
      <additional_description >
        <output_name value="object_path" />
      </additional_description>
    </timeline>

    <fields>
      <field
        input="ComputerName"
        output="computer_name"
        parser="String"
        description="Host name that produced the GetObjInfo report"
      />

      <field
        input="OperatingSystem"
        output="operating_system"
        parser="String"
        description="Operating system version string (e.g., 'Windows Server 2016 Standard Edition')"
      />

      <field
        input="ObjectType"
        output="object_type"
        parser="String"
        description="Type of Windows object (Mutant, Type, Directory, SymbolicLink, etc.)"
      />

      <field
        input="ObjectName"
        output="object_name"
        parser="String"
        description="Name of the Windows object in the namespace"
      />

      <field
        input="ObjectPath"
        output="object_path"
        parser="String"
        description="Full path of the object in the object manager namespace"
      />

      <field
        input="LinkTarget"
        output="link_target"
        parser="String"
        description="Target path that a symbolic link points to (empty if not applicable)"
      />

      <field
        input="LinkCreationTime"
        output="link_creation_time"
        parser="DateTime"
        description="Timestamp when the link was created"
      />

      <field
        input="Description"
        output="description"
        parser="String"
        description="Additional description or metadata about the object"
      />
    </fields>
  </mapping>

</plugin>
